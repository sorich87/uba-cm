body {
  padding-top: 60px;
  padding-bottom: 40px;
}
.brand {
  background: url(../images/logo.gif) no-repeat;
  text-indent: 100%;
  white-space: nowrap;
  overflow: hidden;
  width: 40px;
}
.sidebar-nav {
  padding: 9px 0;
}
